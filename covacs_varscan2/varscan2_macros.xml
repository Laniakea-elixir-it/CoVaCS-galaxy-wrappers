<macros>
  <macro name="reference_loc">
      <conditional name="reference_source">
            <param name="reference_source_selector" type="select" label="Will you select a bed file from your history or use a built-in bed?" help="Built-ins were indexed using default options. See `Indexes` section of help below">
                <option value="cached">Use a built-in bed</option>
                <option value="history">Use a bed from history as reference</option>
            </param>
            <when value="cached">
                <param name="ref_file" type="select" label="Using reference bed" help="Select bed from the list">
                    <options from_data_table="covacs_bed">
                        <filter type="sort_by" column="2" />
                        <validator type="no_options" message="No indexes are available" />
                    </options>
                    <validator type="no_options" message="A built-in reference genome is not available for the build associated with the selected input file"/>
                </param>
            </when>
            <when value="history">
                <param name="reference" type="data" format="bed" label="Use the following dataset as reference bed " help="You can upload a bed file to the history and use it" />
            </when> 
      </conditional>
  </macro>
</macros>
